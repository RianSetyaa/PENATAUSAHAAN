# SIM-TKD - Blokir akses langsung ke file SQL / database dari browser
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
